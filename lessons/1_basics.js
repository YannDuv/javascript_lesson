'use strict';

var lesson = {};

// The method should return a famous String
lesson.sayHelloWorld = function() {

};

// Ok... Can you create a method that should return the length of the result of the last method
/*lesson.getHelloWorldLength = function() {

};*/

// Fine! But now we want some logic! So the current method should return the sum of the two arguments
/*lesson.addTwoNumbers = function(a, b) {

};*/

// Now the function has to return a function that make the sum of two arguments
/*lesson.getFunctionThatAddTwoNumbers = function() {

};*/

// Just to be sure...
// Can you make a method that return a function that make the substraction of the first arg. by the second arg?
/*lesson.getFunctionThatDoASubstraction = function() {

};*/

// Ok let's try some operators : can you fill the answers with true or false?
// Without cheating of course -_-
/*lesson.multipleChoiceQuestion = function() {
  var answer = [];

  // exemple
  // what is the result of :
  // ( 'hello world'.replace("hello", "goodbye") === 'goodbye world' ) && ( "hello".length === 2 )
  answer[0] = false;

  // 'hel' + 'lo' === 'hello' || parseInt('6') === '6'
  //answer.push(yourReponseHere);

  // !true
  //answer.push(yourReponseHere);

  // typeof 6 === 'number'
  //answer.push(yourReponseHere);

  // typeof (typeof 6) !== 'string'
  //answer.push(yourReponseHere);

  // true === !lesson.sayHelloWorld
  //answer.push(yourReponseHere);

  // true !== myDeclaratedVar
  //answer.push(yourReponseHere);

  // true === !undefined
  //answer.push(yourReponseHere);

  // false === undefined
  //answer.push(yourReponseHere);

  // lesson.addTwoNumbers(6, 3)  === lesson.addTwoNumbers(6, 3, 12)
  //answer.push(yourReponseHere);

  // 6 <= 3
  //answer.push(yourReponseHere);

  // This one for the fun
  // 1/3 === 0.3333333333333333
  //answer.push(yourReponseHere);

  //And oh! another
  //0.1 + 0.2 === 0.3
  //answer.push(yourReponseHere);

  return answer;
};*/

// Ok.. last question was an introduction to arrays
// Let's create an method that take two arrays of the same size and return an array with the multiplication of the arrays argument's cells
// ex : returnAnObjectWithOneProperty( [2, 6], [6, 4] ) = [12, 24]
// if needed check for javascript loop on the internet
/*lesson.multiplyTwoArrays = function(array1, array2) {

};*/

exports.Lesson = lesson;
